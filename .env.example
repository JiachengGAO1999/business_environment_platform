# 全球营商环境智能分析平台 — 环境变量
# 仅 demo 用，不包含真实密钥

# Agent 模式：mock（默认）| http（后续对接真实 API）
VITE_AGENT_MODE=mock

# 真实 Agent API 地址（http 模式时才需要）
VITE_AGENT_API_URL=

# 注意：真实 AGENT_API_KEY 只能放后端环境变量，不要在前端 .env 中暴露
